One-line: Labelled checkbox for consent and opt-ins (newsletter, "I'd like to sponsor a resident").

```jsx
<Checkbox label="Keep me updated with rescue stories" defaultChecked />
```

Standard input props pass through (`checked`, `onChange`, `name`, `disabled`).
